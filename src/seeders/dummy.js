'use strict';
module.exports = {
  up: async (query) => {
    /**
     * Add seed commands here.
     *
     * Example:
     * await query.bulkInsert('People', [{
     *   name: 'John Doe',
     *   isBetaMember: false
     * }], {});
    */
   
  },
  down: async (query) => {
    /**
     * Add commands to revert seed here.
     *
     * Example:
     * await query.bulkDelete('People', null, {});
     */
  }
};
