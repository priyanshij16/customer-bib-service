'use strict'

const Sequelize = require('sequelize')
module.exports = {
  up: async (query) => {
    
  
  },

  down: async (query) => {
    /**
     * Add reverting commands here.
     *
     * Example:
     * await queryInterface.dropTable('users');
     */
    
  }
};
